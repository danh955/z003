<Page
    x:Class="HilresStock.Views.ChartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HilresStock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <!-- Top bar -->
        <Grid Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Padding="5" Background="LightGray" BorderThickness="1,0,0,1" BorderBrush="Gray">
            <TextBlock Text="Chart page" />
        </Grid>
        <!-- Left column -->
        <Grid Grid.Column="0" Grid.Row="1" Padding="5" Background="Honeydew" BorderThickness="1,0,1,0" BorderBrush="Gray" Height="Auto">
            <TextBlock Text="Left side." />
        </Grid>
        <!-- Right area -->
        <Grid Grid.Column="1" Grid.Row="1" Height="Auto" Padding="5">
            <TextBlock Text="Right side." />
        </Grid>
    </Grid>
</Page>